export type SelectOption = {
  id: string;
  label: string;
};

export type UserRegistrationData = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
  birthDate: string;
  street: string;
  city: string;
  country: string;
  postalCode: string;
};
